#ifndef INPUTCHECK_H
#define INPUTCHECK_H

#include <string>

using namespace std;

class inputCheck {
public:
     // check if a given file is empty
     bool isFileEmpty(const string& filename);
private:

};

#endif